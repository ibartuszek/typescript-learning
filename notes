// install typescript globallz
npm install -g typescript

// check more info on:
www.typescript.org/Handbook

// To compile a ts file
tsc myTypeScript.ts

// To compile more ts files together:
tsc util.ts timesTwo.ts --out util.js

// To run a javascript locally
node myJavascript.js

// grunt-ts:
https://www.npmjs.com/package/grunt-ts
steps:
- npm init
- npm install typescript --save
- npm install grunt --save
- npm install grunt-cli --save
- npm install grunt-ts --save

// extra plugins for vscode:
- ES7 React/Redux/Graphql:
  - shortcuts:
    - rfc: react functional component
    - clg: console.log()
- FireCode font special mathematical signs instead of  === or !==
- Import cost shows to imprt size in kb
- Prettier: ctrl + shift + p

// create react-app with ts:
- npm i create-react-app // create react app
- npx create-react-app my-app-ts // install dependencies, packages, create sources
- npm i typescript @types/node @types/react @types/react-dom

// creae react-app with ts:
- create new project dir and go into it
- npm init -y // init default project (in created project dir: package.json)
- create empty src folder with index.tsx file
- create .bablerc, index.html, webpack.config.js
- add basic settings and content
- npm i @babel/core @babel/preset-env @babel/preset-react @babel/preset-typescript webpack webpack-cli webpack-dev-server babel-loader react react-dom @types/react @types/react-dom

// router:
- https://reach.tech/router/
- https://github.com/reach/router